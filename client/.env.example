# 注意：前端开发端口（CLIENT_PORT）与后端端口（PORT）统一在仓库根 .env 配置，
# vite 会自动读取并联动 server.port 与代理 target，通常无需在本文件配置。
#
# 高级覆盖：仅当需要把前端代理指向非本仓库后端（如远程联调）时，才取消下行注释。
# 缺省由根 .env 的 PORT 派生（http://localhost:<PORT>）。
# VITE_API_PROXY_TARGET=http://localhost:3000
