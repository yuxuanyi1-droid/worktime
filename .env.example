# ===== 前后端端口配置（单一真相源）=====
# 修改端口只需改这里，前后端、CORS、代理会自动联动。
# 复制本文件为 .env（根目录），改 PORT / CLIENT_PORT 即可。
#
# - PORT：        后端 API 服务端口（对应 server/src/app.ts 监听）
# - CLIENT_PORT： 前端 Vite 开发服务器端口（自动写入后端 CORS 白名单 + vite server.port）
#
# 注意：后端业务配置（JWT、DB_PATH 等）仍在 server/.env，保持就近内聚；
#       端口在这里配置优先级最高（dotenv 默认不覆盖）。

# 后端 API 服务端口
PORT=3000
# 前端开发服务器端口
CLIENT_PORT=5173
# 子路径部署前缀（如 /worktime）：整个应用挂在该子路径下。
# 空 = 根路径部署（默认）；前端需用相同 BASE_PATH 重新 build，后端重启生效。
# 详见 README「子路径部署」。
# BASE_PATH=/worktime
